services:
  db:
    image: postgres:15
    container_name: main_db
    restart: always
    env_file:
      - ./Backend/.env
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"

  backend:
    build: ./Backend
    env_file:
      - ./Backend/.env
    environment:
      - TZ=Europe/Moscow
    container_name: main_backend
    restart: always
    ports:
      - "8000:8000"
    volumes:
      - ./Backend:/app
      - ./Backend/speech_to_text/model:/app/speech_to_text/model
      - ./Backend/speech_to_text/silero_vad:/app/speech_to_text/silero_vad
    depends_on:
      - db

volumes:
  postgres_data: